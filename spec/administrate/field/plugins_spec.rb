require "spec_helper"

describe Administrate::Field::Plugins do
  it "has a version number" do
    expect(Administrate::Field::Plugins::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
